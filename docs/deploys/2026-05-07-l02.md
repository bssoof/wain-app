# L-02 Deploy 2026-05-07

## Summary
- Pre-deploy tag: `pre-deploy-2026-05-07-l02`
- Deploy command: `npx firebase deploy --only hosting`
- Project: `wain-d2e28`
- Duration: 327 seconds

## What Changed
- CSP moved from report-only-only to an enforced baseline.
- A stricter `Content-Security-Policy-Report-Only` policy is also emitted in parallel for tuning.
- `upgrade-insecure-requests` moved to enforced CSP only; it is no longer present in report-only CSP.
- `Strict-Transport-Security` was added in code/config as `max-age=31536000; includeSubDomains` without `preload`.
- `firebase.json` was also aligned because `wain-admin` hosting headers are a deployed header source.

## Validation
- Admin web Vitest: 699 passed across 118 files.
- Admin web TSC: 0 errors.
- Admin web build: passed.
- Local production smoke on `http://localhost:3010/admin/sign-in`: 200, enforced CSP present, report-only CSP present, HSTS present, no Playwright CSP console errors.
- Production probes:
  - `/admin/sign-in`: 200, enforced CSP present, report-only CSP present, HSTS present, no Playwright CSP console errors.
  - `/admin/access-denied?route=dashboard`: 200, enforced CSP present, report-only CSP present, HSTS present.

## Production HSTS Note
The repo configuration intentionally omits `preload`. The `web.app` production response returns Firebase-managed HSTS as `max-age=31556926; includeSubDomains; preload`, while the deployed config sent to Firebase contains `max-age=31536000; includeSubDomains`. This appears to be platform behavior for Firebase `web.app` domains rather than a repo-level `preload` directive.

## Rollback

```powershell
git checkout pre-deploy-2026-05-07-l02
cd admin_web_console
npx firebase deploy --only hosting
```

## Manual Verification Required
- [ ] Login at `https://wain-admin.web.app/admin/sign-in`.
- [ ] Open browser DevTools console.
- [ ] Verify no `Refused to execute inline script` or similar CSP errors.
- [ ] Navigate dashboard, finance, content, and config; verify all load.
- [ ] Check Network tab for CSP report requests if a report endpoint is configured.
- [ ] If any CSP error blocks UI, rollback immediately.

## Follow-Ups
- Configure `report-uri` or `report-to` to collect CSP violations.
- After report-only data is stable, tighten enforced CSP by removing `unsafe-eval`, replacing `https:` wildcard with explicit origins, and considering nonce-based inline script handling.
- Do not pursue HSTS preload until the custom domain posture is stable and explicitly approved.
