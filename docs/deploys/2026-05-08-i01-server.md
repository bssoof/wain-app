# I-01 server.ts Production Gate Deploy 2026-05-08

## Summary
- Pre-deploy tag: `pre-deploy-2026-05-08-i01-server`
- Deploy command: `npx firebase deploy --only hosting`
- Project: `wain-d2e28`
- Duration: 329 seconds

## What changed
- `admin_web_console/lib/firebase/server.ts`: in production (`NODE_ENV === "production"`), credential resolution always uses `applicationDefault()`.
- JSON service-account key path lookup is skipped entirely in production.
- Local/dev/test behavior is unchanged: JSON key fallback remains available outside production.

## Validation
- Focused web Vitest: 3 passed in `lib/firebase/server.test.ts`
- Admin web Vitest: 703 passed across 119 files
- Admin web TSC: 0 errors
- Admin web build: passed
- Local production smoke: skipped because local ADC was not configured at `C:\Users\a-z\AppData\Roaming\gcloud\application_default_credentials.json`
- Post-deploy probes:
  - `/admin/sign-in` -> 200
  - `/admin/access-denied?route=dashboard` -> 200

## Why
Cloud Run SSR already runs on ADC. Gating prevents future regressions where a JSON key file might land in a Cloud Run image.

## Rollback

```powershell
git checkout pre-deploy-2026-05-08-i01-server
cd admin_web_console
npx firebase deploy --only hosting
```

## Manual verification
- [ ] Sign in to `https://wain-admin.web.app/admin/sign-in`
- [ ] Visit dashboard, finance, content, config, and venues to confirm SSR works
- [ ] Check Cloud Run logs for any credential initialization errors

## Follow-ups
- Phase A: update runbooks to ADC-first and remove stale local probe scripts
- Phase B: pilot script ADC migration, starting with `functions/scripts/seed_transport_mvp.js`
