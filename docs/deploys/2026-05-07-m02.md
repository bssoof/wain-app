# M-02 Deploy 2026-05-07

## Summary
- Pre-deploy tag: pre-deploy-2026-05-07-m02
- Deploy command: `npx firebase deploy --only functions,firestore:rules`
- Project: wain-d2e28
- Duration: 154 seconds

## What Changed
- `inputFiles` are validated against `gs://<default-bucket>/venues/{venueId}/photos/menu_import_`.
- File count is limited to 20.
- Duplicate `inputFiles` entries are rejected.
- Direct client writes to `menu_import_jobs.input_files` are blocked.
- OCR dispatch re-validates job URIs before calling Vision API or emulator OCR mocks.

## Validation
- Functions top-level: 29 passed
- Functions TSC: 0 errors
- Menu import emulator pipeline: 20 passed
- Firestore rules: 30 passed
- Admin web Vitest: 696 passed across 118 files
- Admin web TSC: 0 errors

## Rollback Plan

```powershell
git checkout pre-deploy-2026-05-07-m02
cd admin_web_console
npx firebase deploy --only functions,firestore:rules
```

## Manual Verification
- [ ] Test legitimate menu import flow as merchant works end-to-end.
- [ ] Verify cross-venue `inputFiles` is rejected.
- [ ] Monitor Functions error rates for 30 minutes.
