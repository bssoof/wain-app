# Venue not_active Error Separation Deploy 2026-05-08

## Summary
- Pre-deploy tag: `pre-deploy-2026-05-08-venue-msg`
- Deploy command: `npx firebase deploy --only functions:adminUpdateVenueVisibility,hosting`
- Project: `wain-d2e28`
- Duration: 263 seconds

## What changed
- `adminUpdateVenueVisibility` now throws:
  - `venue_not_active` when `operational_status != active`
  - `venue_expected_state_conflict` only on real expected-state drift
- Admin web sends the row's current operational status in the visibility command expected state, so known non-active rows can receive the clearer `venue_not_active` message.
- `admin-localization` adds the Arabic message for `venue_not_active`.

## Validation
- Functions top-level: 36 passed
- Functions TSC: 0 errors
- Venue callable emulator flow: 29 passed
- Admin web targeted Vitest: 6 passed across 2 files
- Admin web TSC: 0 errors

## Rollback

```powershell
git checkout pre-deploy-2026-05-08-venue-msg
cd admin_web_console
npx firebase deploy --only functions:adminUpdateVenueVisibility,hosting
```

## Manual verification
- [ ] Try toggling visibility on a non-active venue: should show `لا يمكن تنفيذ العملية لأن حالة الجهة ليست نشطة.`
- [ ] Real state drift case still shows the original conflict message.
