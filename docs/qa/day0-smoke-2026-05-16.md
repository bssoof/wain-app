# Day 0 Smoke - 2026-05-16

## Scope

Day 0 functional smoke on Firebase Emulator Suite using the release APK built from commit `7a84f814`.

## Environment

- Git tag at start: `qa-final-2026-05-15-p2`
- Firebase project namespace: `wain-d2e28`
- Android target: `emulator-5554`
- APK: `build/app/outputs/flutter-apk/wain-qa-7a84f814-release.apk`
- Emulator hosts:
  - Auth: `127.0.0.1:9099`
  - Firestore: `127.0.0.1:8080`
  - Functions: `127.0.0.1:5001`
  - Storage: `127.0.0.1:9199`

## Seed

Seed command:

```powershell
node scripts/qa-seed.mjs --project=wain-d2e28 --reset --out=.tmp/qa-seed-output.json
```

Final seed output:

- `seedRunId=qa_seed_2026-05-16T00-13-29-987Z`
- `projectId=wain-d2e28`
- `accounts=7`
- `venues=2`
- `wallets=2`

During smoke, the consumer discovery flow exposed that QA venues were not discoverable. Root cause was seed schema drift:

- `operational_status` was seeded as `open`, while app discovery filters require `active`.
- `tags` was missing, but `Venue.fromJson` requires it when venues are parsed.
- `city_key` was missing, reducing deterministic city lookup coverage.

The seed now writes discoverable venue fixtures with `operational_status=active`, `city_key=ramallah`, and complete `tags`/`all_tags`.

## Consumer Smoke

Path:

1. Clean install release APK.
2. Grant notification permission and accept location permission.
3. Complete onboarding/wizard via `تخطي`.
4. Open recommendations via `شوف الاقتراحات`.
5. Verify seeded venues appear.
6. Open first venue card.

Result: `PASS`

Evidence:

- Recommendations showed `مطعم QA الأول`.
- Venue details opened successfully.
- Final details screenshot: `.tmp/wain-consumer2-card-open.png`
- Visible detail signals:
  - `مطعم QA الأول`
  - `QA Restaurant One`
  - `العروض والمنيو`
  - `خصم QA 20%`
  - `اتصال`
  - `واتساب`
  - `توجيه`

## Merchant Smoke

Path:

1. Clean install release APK.
2. Complete onboarding/wizard enough to reach settings.
3. Open `الإعدادات`.
4. Sign in with `merchant.qa.01@wain.test`.
5. Verify post-auth landing.

Result: `PASS`

Evidence:

- Auth emulator `lastLoginAt` changed from `1778890411001` to `1778890748291`.
- Logcat contained `User signed in with email: merchant.qa.01@wain.test`.
- Final screen was merchant dashboard, not consumer landing.
- Final dashboard screenshot: `.tmp/wain-merchant-final.png`
- Visible dashboard signals:
  - `لوحة التاجر`
  - `مطعم QA الأول`
  - `رصيد وين`
  - `إدارة العروض`

## Financial Verifiers

Finance:

```text
[PASS] wallet_finance_invariants_ok: Wallet finance invariants passed. (venueId="venue_qa_01" entries=6 availableBalance=500)
[PASS] wallet_finance_invariants_ok: Wallet finance invariants passed. (venueId="venue_qa_02" entries=6 availableBalance=500)

QA finance verifier summary: wallets=2 fail=0 warn=0
FINANCE_EXIT=0
```

Audit:

```text
[PASS] wallet_audit_trail_ok: Wallet audit trail checks passed. (auditEvents=26 venues=["venue_qa_01","venue_qa_02"])

QA audit verifier summary: audit_events=26 fail=0 warn=0
AUDIT_EXIT=0
```

## Verdict

`PASS_DAY0_BASELINE`

The emulator-first QA baseline is ready for the next Day 0 pass: financial UI actions for top-up/reversal followed by the same finance and audit verifiers.
