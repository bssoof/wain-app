# خطة تنفيذ "محفظة وين" (Wain Wallet)

## Goal Description
الهدف هندسة وبناء نظام "محفظة داخلية" (Wallet System) خاص بتطبيق "وين" لتنفيذ عمليات الدفع للميزات الإضافية (مثل ترويج القصص) بكل سهولة بدون الحاجة لربط بوابات الدفع الإلكترونية (Payment Gateways).
الفكرة تعتمد على أن يقوم صاحب المتجر بتحويل مبلغ مالي للحساب البنكي الخاص بإدارة التطبيق، وبناءً عليه تقوم الإدارة بوضع هذا الرصيد في "محفظته الرقمية" داخل التطبيق. ليتمكن من استخدام الرصيد في العمليات المدفوعة مستقبلاً.

## هيكلية النظام (Architecture)
نظام المحفظة يجب أن يُبنى بحيث يضمن **أمان الأرصدة** واستحالة التلاعب بها من جهة العميل (التطبيق).
بناءً على ذلك، يتم تصميم النظام كالتالي:

1.  **Firebase Firestore (قاعدة البيانات):**
    *   **جدول المحفظة `merchant_wallets`:** وثيقة لكل فرع/متجر تحتوي على रصيده الحالي `balance` (يتم تحديثه فقط عبر الـ Cloud Functions).
    *   **جدول حركات المحفظة `wallet_transactions`:** سجل غير قابل للتعديل (Append-Only) يسجل كل حركة (إيداع من الإدارة، سحب لترويج قصة).

2.  **Firebase Cloud Functions (السيرفر):**
    *   تطبيق دوال سحابية تتعامل مع منطق الدفع.
    *   عند طلب "ترويج ستوري"، الدالة السحابية تتحقق من توفر رصيد (مثلاً 3 وحدات). إذا توفر، تقوم بخصم الرصيد، وإنشاء سجل خصم في المعاملات، وتنفيذ الترويج، كل ذلك داخل عملية واحدة (Firestore Transaction) لضمان عدم حدوث أخطاء إذا انقطع الاتصال أو أرسل أكثر من طلب بنفس الوقت.

3.  **Flutter App (التطبيق):**
    *   شاشة **"محفظة وين"** تعرض الرصيد الحالي للمتجر وسجل الحركات السابقة.
    *   **شاشة القصص (Stories):** عند محاولة الترويج، يتم عرض رصيد المحفظة. إذا لم يكن الرصيد كافياً، يظهر تنبيه يعلم التاجر بكيفية شحن المحفظة، وإلاّ يتم سحب الرصيد وترويج القصة بنجاح مع إظهار إشعار مميز.

---

> [!CAUTION]
> ## User Review Required - نقاط أساسية للمراجعة
> **أمان المحفظة (Security First):** تطبيق هذه الميزة سيتطلب إغلاق صلاحيات الكتابة المباشرة على جدول المحفظة من التطبيق (عبر Firestore Security Rules) لكي لا يتمكن أي متجر من زيادة رصيده من جهازه، بحيث يتم الشحن والخصم حصرياً عبر الـ Cloud Functions الآمنة!
> هل توافق على أن يكون الشحن (من قبلكم כإدارة) مبدئياً عن طريق تعديل مباشر من لوحة تحكم فايربيس (Firebase Console) أو سكربت جاهز، إلى حين بناء لوحة تحكم للإدارة مستقبلاً؟

---

## Proposed Changes (التغييرات التفصيلية)

### 1. Data Models (طبقة البيانات - Flutter)
#### [NEW] `lib/features/merchant/domain/entities/merchant_wallet.dart`
- إنشاء Data Class يمثل المحفظة (معرف المتجر، وقيمة الرصيد).

#### [NEW] `lib/features/merchant/domain/entities/wallet_transaction.dart`
- إنشاء Data Class يمثل حركة المحفظة (إيداع/سحب، القيمة، التاريخ، وصف العملية).

### 2. Repositories (طبقة المستودعات - Flutter)
#### [NEW] `lib/features/merchant/data/repositories/merchant_wallet_repository.dart`
- عمل دوال للحصول على الرصيد بالوقت الفعلي (Realtime Stream) باستخدام دعم الـ Offline للتمكن من رؤية الرصيد بدون إنترنت (مع الاعتماد الكلي على السيرفر وقت الخصم).

#### [MODIFY] `lib/features/merchant/data/repositories/merchant_stories_repository.dart`
- تعديل دالة `promoteStory` لترسل طلب الترويج إلى Backend وتتعامل مع خطأ "Insufficient Balance" (رصيد غير كافٍ).

### 3. Providers (إدارة الحالة - Riverpod)
#### [NEW] `lib/features/merchant/presentation/providers/merchant_wallet_providers.dart`
- إضافة الـ Providers الخاصة بجلب وتوفير معلومات الرصيد والحركات عبر طبقات التطبيق.

### 4. UI Screens (واجهة الطبيق)
#### [NEW] `lib/features/merchant/presentation/screens/merchant_wallet_screen.dart`
- تصميم شاشة رائعة ومريحة، تعرض:
    1. الرصيد الحالي بشكل بارز مع بطاقة مميزة (مثلاً بلون التطبيق الأساسي).
    2. نص يوضح كيفية الشحن (مثل: "لشحن الرصيد، يرجى تحويل مبلغ لحسابنا عبر تطبيق Клиق أو حوالة بنكية، وتواصل معنا لإضافة الرصيد").
    3. قائمة بسجل الحركات (إيداع الإدارة، سحب لترويج ستوري...).

#### [MODIFY] `lib/features/merchant/presentation/screens/merchant_dashboard_screen.dart`
- إضافة زر أو قسم في لوحة تحكم التاجر للولوج إلى "محفظة وين" بوضوح.

#### [MODIFY] `lib/features/merchant/presentation/screens/merchant_stories_screen.dart`
- قبل أو أثناء رفع طلب الـ Promote للستوري، إضافة لوجيك يتحقق من أنه إذا كان الرصيد < التكلفة المطلوبة (3 مثلاً)، يتم إظهار `AlertDialog` واضح يخبره: "عذراً رصيد المحفظة غير كافٍ. هل تود الذهاب للمحفظة لشحنها؟".

### 5. Backend (Firebase Cloud Functions)
#### [MODIFY] Backend Source Code (Functions)
- تحديث الدالة السحابية الخاصة بـ `promoteStory` (التي تستدعى في `MerchantStoriesRepository` حالياً):
  - قراءة سطر المحفظة من `merchant_wallets/{venueId}`.
  - إذا كان الرصيد أقل من التكلفة المحددة للأيام (مثلاً يوم الترويج بـ 1 وحدة، 3 أيام بـ 3 وحدات)، إرجاع خطأ `failed-precondition` برسالة `insufficient_balance`.
  - إذا توفر الرصيد، يتم خصم الرصيد بإنقاصه، وإضافة سجل في `wallet_transactions`، ثم تنفيذ أثر الـ Promote.

---

> [!TIP]
> ## وجهة نظري التقنية والاستراتيجية
> استخدام **محفظة داخلية (Credits System)** هو ذكي جداً لعدة أسباب:
> 1. **وفرت عمولات البنوك** (Payment Gateways تأخذ عمولة على كل عملية).
> 2. **تجربة مستخدم سريعة جداً (Frictionless)** للتاجر، لأنه ضغطة زر واحدة "إبدأ الترويج" ويتم الخصم بثانية دون الحاجة لفتح شاشات دفع وإدخال بطاقة كل مرة.
> 3. **تطبيق مبدأ (Lock-in/Microtransactions):** التاجر ممكن أن يحول 100، وبهذا تضمن أنه سيتفاعل مع الميزات المدفوعة على مدار أشهر قادمة بكل سهولة.
>
> **سعراً للميزات:** بدلاً من عرض الخصم برمز العملة (مثل 3 دنانير)، أنصح بجعلها بنظام "النقاط" أو "العملة الخاصة" (مثلاً المحفظة فيها 100 كوين/نقطة)، وترويج القصة يكلف 3 نقاط. هذا النظام يُشعر المستخدم نفسياً بمرونة أكبر من الدفع بالمال المباشر.

---

## Open Questions
1. كيف تفضل أن تظهر وحدة رصيد المحفظة للتاجر؟ هل تظهر كعملة فعلية (مثلاً: رصيدك 100 دينار/دولار) أم كرصيد نقاط المحفظة (مثلاً: رصيدك 100 نقطة وين)؟
2. دالة إيداع الرصيد: في المرحلة الأولى، هل ستقوم الإدارة (أنت) بفتح لوحة Firebase لإضافة الرصيد للتاجر يدوياً، أم تود أن أكتب لك سكربت صغير/ميزة للإدارة لتقوم بإضافة الأرصدة منه؟
3. هل تكلفة الترويج الحالية للستوريات ثابتة؟ (مثلاً يوم واحد = X من الرصيد، 3 أيام = Y).

## Verification Plan
1. سنقوم ببناء مكونات الواجهة الأمامية وإضافتها للتطبيق.
2. سنعدل الـ Cloud Functions لتقوم بعملية الـ Transaction الآمنة.
3. سنختبر حالة "المحفظة فارغة" (والتأكد من عدم قدرته على الترويج).
4. سنختبر محاكاة شحن المحفظة، والتأكد من خصم الرصيد بنجاح بعد عملية الترويج، وحفظ الحركة في السجل.
